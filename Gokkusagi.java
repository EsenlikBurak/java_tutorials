
import acm.graphics.*;
import acm.program.*;
import java.awt.*;

public class Gokkusagi extends GraphicsProgram {

	public void run() { 
		GRect arkaPlan = new GRect(0,0,getWidth(), getHeight());
		arkaPlan.setColor(Color.CYAN);
		arkaPlan.setFillColor(Color.CYAN);
		arkaPlan.setFilled(true);
		add(arkaPlan);

		for (int i = 0; i < 7; i++){
			// kodunuzu buraya yaziniz
		}
	}
}